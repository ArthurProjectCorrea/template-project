# Exemplos de Uso

## Requisição via curl

```bash
curl http://localhost:3000/
```

## Consumo do endpoint em JavaScript

```js
fetch("http://localhost:3000/")
  .then((res) => res.text())
  .then(console.log);
```
